# KiCAD MCP Server - Python Dependencies
# Production dependencies only

# KiCAD Python API (IPC - for future migration)
# kicad-python>=0.5.0  # Uncomment when migrating to IPC API

# Schematic manipulation
kicad-skip>=0.1.0

# Image processing for board rendering
Pillow>=9.0.0

# SVG rendering
cairosvg>=2.7.0

# Colored logging
colorlog>=6.7.0

# Data validation (for future features)
pydantic>=2.5.0

# HTTP requests (for JLCPCB/Digikey APIs - future)
requests>=2.32.5

# Environment variable management
python-dotenv>=1.0.0

# Optional: real-time autoplacer visualization (commands/autoplacer_viz.py).
# Used during interactive parameter tuning in an iPython session; not
# required for the MCP server itself.  Lazy-imported at viz instantiation
# so non-viz callers don't pay the load cost.
matplotlib>=3.7

# Routing-topology analysis (commands/topology.py): distance transform +
# connected-components labelling for analyze_routable_regions /
# check_pad_routability. Pulls numpy as a transitive dep.
scipy>=1.11

# Routing-topology polygon-exact mode (check_pad_routability(mode="exact"))
# — Phase 4c verification path. Pulls numpy as a transitive dep.
shapely>=2.0
